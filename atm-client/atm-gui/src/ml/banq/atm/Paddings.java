package ml.banq.atm;

// The static paddings class
public class Paddings {
    public static final int LARGE = App.getInstance().getWindowWidth() / 100 * 2;
    public static final int NORMAL = App.getInstance().getWindowWidth() / 100 * 1;
}
